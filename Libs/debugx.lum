
function print_stack(msg, level)
	level = level or 2
	local trace = debug.traceback(msg, level)
	print(_text(trace))
end

function default_watch(old, new)

end

function watch(object, name, ...)
	
end
