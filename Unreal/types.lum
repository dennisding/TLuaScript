
import('type_map')

class('types')

function types:_init()
	self._types = {}  -- {name: ctypes}
	self._invalids = {} -- {name: true}
end

function types:__index(name)
	local full_name = type_map[name] or name
	local ctype = _cpp_generate_type(full_name)
end

_set_global('Types', types())
