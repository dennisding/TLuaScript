

function _init(self)
	self.IntValue = 1234
	print(_text('float value from cpp'), _text(tostring(self.FloatValue)))
	self.StringValue = _text('从脚本来的中文')

	print(_text('string value from cpp'), self.StringValue)

	self:SayHello(42, _text('请说句话'))
end

function test_hello(self, msg, pos)
	local mt = getmetatable(pos)
	print(_text('test_hello'), _text(pos.__tostring(pos)))
	pos = pos + pos
	print(_text("msg from cpp"), msg, _text(tostring(pos)), _text(pos.x))
--	tablex.print(pos)
end