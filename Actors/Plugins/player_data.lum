
name = data_types.str(_text(''), 'save')
max_hp = data_types.i32(100, 'save')
max_mp = data_types.i32(1000, 'save')
hp = data_types.i32(100, 'save')
mp = data_types.i32(1000, 'save')

-- inventory = data_types.inventory()

function _init(self)
	self.name = _text('勇者(hero)')
end

function setup_user_data(self)

end

function on_set_name(self, name)
	print(_text('on_set_name'), towstring(name))
	_gui.main:set_name(name)
end
